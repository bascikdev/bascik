{
  "name": "bascik-docs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "author": "Collin Thomas",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "dev": "BASCIK_SITE_URL=https://bascik.dev bascik",
    "build": "BASCIK_SITE_URL=https://bascik.dev bascik --build",
    "serve": "bascik --server",
    "preview": "bascik --server",
    "e2e": "playwright test --config e2e/playwright.config.ts --reporter=line",
    "pkg-dev": "node --inspect --watch-path=./bascik.config.ts --watch-path=../../pkg/src ../../pkg/src/transpile.ts",
    "generate:llms": "node scripts/generate-llms-txt.ts",
    "generate:og": "node scripts/generate-og-images.ts",
    "generate:search": "node scripts/generate-search-index.ts",
    "lhci": "lhci",
    "lighthouse": "BASCIK_SITE_URL=https://bascik.dev bascik --build && lhci autorun --config=./lighthouse/lighthouserc.json && lhci autorun --config=./lighthouse/lighthouserc.light.json",
    "lighthouse:all": "BASCIK_SITE_URL=https://bascik.dev bascik --build && node lighthouse/generate-config.ts && lhci autorun --config=./lighthouse/lighthouserc.all.json && lhci autorun --config=./lighthouse/lighthouserc.all-light.json",
    "lighthouse:ci": "BASCIK_SITE_URL=https://bascik.dev bascik --build && lhci autorun --config=./lighthouse/lighthouserc.json && lhci autorun --config=./lighthouse/lighthouserc.light.json",
    "typecheck": "yarn workspace @bascik/bascik build && tsc --noEmit",
    "unit": "yarn workspace @bascik/bascik build && vitest run",
    "test": "yarn workspace @bascik/bascik build && vitest run",
    "coverage": "yarn workspace @bascik/bascik build && vitest run --coverage.enabled && node scripts/update-coverage.ts",
    "cover": "yarn coverage",
    "update-coverage": "yarn workspace @bascik/bascik build && vitest run --coverage.enabled && node scripts/update-coverage.ts"
  },
  "devDependencies": {
    "@lhci/cli": "^0.14.0",
    "@playwright/test": "^1.62.0",
    "@resvg/resvg-js": "^2.6.2",
    "@types/canvas-confetti": "^1",
    "@types/node": "^24.0.0",
    "@types/nunjucks": "^3",
    "@vitest/coverage-v8": "^4.1.10",
    "canvas-confetti": "^1.9.4",
    "esbuild": "^0.28.2",
    "lighthouse": "^12.4.0",
    "opentype.js": "^2.0.0",
    "sharp": "^0.35.3",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@bascik/bascik": "workspace:*",
    "ejs": "^6.0.1",
    "handlebars": "^4.7.9",
    "marked": "^18.0.7",
    "nunjucks": "^3.2.4"
  }
}
